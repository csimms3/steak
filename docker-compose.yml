services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: steak
      POSTGRES_PASSWORD: steak
      POSTGRES_DB: steak
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
