node_modules
.env
.next
/src/generated/prisma
prisma/config.ts

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Local agent / editor config — not for version control
.claude/
.cursor/


# Local env overrides (Next.js loads .env*.local) — may hold real secrets
.env*.local

# Local tool state
.freebuff/
